Angel｜幸福教養課程工作台 v1（完整覆蓋 ZIP）

【你要做的只有 3 步】
1) 把你的 GAS Web App URL（/exec）貼到右上角「設定」
2) 按「選工具」→ 勾主工具/副工具 → 套用
3) 填主題 → 按「存回後臺」

【你會看到】
- 右側清單會出現新資料
- AI 指令會自動打包（按「一鍵複製｜AI 指令」）
- TSV 一列也會自動打包（按「一鍵複製｜TSV 一列」）

【重要】
- v1 先做穩：工具勾選 / AI 協作 / 存回試算表
- 下一版 v2 再加：模組課程 / 提案彙整
